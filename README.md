# zitongzhoueric.github.io

Make changes to `index.html`; jekyll statically generates the website under `_site/...` so do not change anything there

To test locally, run: `bundle exec jekyll serve`

(republish Feb 24)
